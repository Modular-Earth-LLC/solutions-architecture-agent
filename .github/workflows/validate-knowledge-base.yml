name: QA Validation

on:
  pull_request:
    branches: [main]
    paths:
      - 'knowledge_base/schemas/**'
      - 'skills/**'
      - 'agents/**'
      - '.claude-plugin/**'
      - 'tests/**'
      - 'CLAUDE.md'
      - '.repo-metadata.json'
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: uv pip install --system jsonschema requests

      - name: Schema validation
        run: python tests/validate_knowledge_base.py

      - name: Consistency validation
        run: python tests/validate_consistency.py

      - name: Plugin structure validation
        run: python tests/test_plugin_structure.py

      - name: Engagement flow validation
        run: python tests/test_engagement_flow.py

      - name: Skill independence validation
        run: python tests/test_skill_independence.py

      - name: Well-Architected validation
        run: python tests/validate_well_architected.py

      - name: End-to-end example validation
        run: python tests/test_end_to_end_example.py

      - name: URL validation
        run: python tests/validate_urls.py
