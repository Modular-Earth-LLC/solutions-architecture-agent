# Slide 10: Future State & Scaling
## Assignment Question Answered: Part 3 Section 2 - Future State Scaling to 20 LOBs

### Claude for PowerPoint Prompt:

"Create a slide titled 'Scaling to 20 LOBs'. In the main body, insert a five-column table with headers 'Approach', 'Cost', 'Isolation', 'Complexity', 'Best When' and the following two rows: Row 1: 'Multi-tenant (shared platform, config per LOB)' | 'Lower' | 'Logical isolation' | 'Lower' | 'LOB rules are similar, cost matters'; Row 2: 'Multi-instance (separate deployments per LOB)' | 'Higher' | 'Physical isolation' | 'Higher' | 'LOBs are very different, or regulation requires it'. Bold 'Multi-tenant' and 'Multi-instance' in the first column. Below the table, add a visually distinct recommendation callout box — use a border or shaded background — containing the text: 'Recommendation: Start multi-tenant with per-LOB configuration. The Autonomize Genesis Platform already supports this — configurable workflows, LOB-specific rules, shared AI engine. If specific LOBs require physical isolation, deploy those as separate instances.' Below the callout, add a smaller caption: 'Honest unknowns: The right answer depends on actual LOB rule complexity and regulatory requirements — both are discovery questions.' Use the existing slide master layout. Speaker notes: Multi-tenant with per-LOB configuration is the starting point. It is simpler, cheaper, and Autonomize's platform already supports it. If specific LOBs need physical isolation, we deploy those separately."

### Diagram to Insert:
None
