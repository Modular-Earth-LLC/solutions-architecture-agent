[project]
name = "solutions-architecture-agent"
version = "1.1.0"
description = "AI solutions architecture agent — Claude Code plugin for the full SA lifecycle"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"

dependencies = [
    "jsonschema>=4.20.0",
    "requests>=2.31.0",
]
